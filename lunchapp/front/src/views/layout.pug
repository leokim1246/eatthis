doctype html
html(lang="ko")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title 점심 추천 MVP
    style.
      body { font-family: system-ui, sans-serif; margin:0; background:#f7f7fb; }
      header, main { max-width: 960px; margin: 0 auto; padding: 16px; }
      header { display:flex; justify-content: space-between; align-items:center; }
      nav a { margin-right:12px; text-decoration:none; color:#333; }
      .card { background:#fff; border:1px solid #eee; border-radius:12px; padding:16px; box-shadow:0 2px 10px rgba(0,0,0,.04); margin-bottom:16px; }
      .btn { display:inline-block; background:#4f46e5; color:#fff; padding:8px 14px; border-radius:8px; text-decoration:none; }
      .btn-ghost { background:transparent; color:#4f46e5; border:1px solid #4f46e5; }
      .grid { display:grid; gap:12px; grid-template-columns: repeat(auto-fit,minmax(260px,1fr)); }
      form label { display:block; margin:8px 0 4px; color:#444; }
      input, select { width:100%; padding:8px; border:1px solid #ddd; border-radius:8px; }
      .error { color:#b91c1c; margin:8px 0; }
  body
    header
      div
        a(href="/") 점심 추천 MVP
      nav
        if user
          a(href="/search") 검색
          a(href="/history") 내 기록
          if user.role==='admin'
            a(href="/admin") 관리자
          form(style="display:inline", method="post", action="/auth/logout")
            button.btn-ghost(type="submit") 로그아웃
        else
          a(href="/auth/login") 로그인
          a(href="/auth/register") 회원가입
    main
      block content

